node_modules
dist
.env
.env.local
*.local
.DS_Store

# Local / private data at repo root only (do NOT use plain "data/" — it ignores src/data/)
/data/

# Generated from local data/recipes.json (see scripts/sync-recipes.mjs)
/public/data/recipes.json

# Tesseract local artifacts
*.traineddata
eng.traineddata
